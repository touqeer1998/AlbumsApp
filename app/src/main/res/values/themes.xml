<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AlbumsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item> <!-- Color of the toolbar -->
        <item name="colorOnPrimary">@color/white_text</item> <!-- Text color on the toolbar -->
        <item name="colorSurface">@color/white</item> <!-- Background color of the app bar -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:autofilledHighlight">@color/transparent</item>
    </style>

    <style name="Theme.AlbumsApp" parent="Base.Theme.AlbumsApp" />
</resources>